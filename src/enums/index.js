const COURSES = require('./courses.enum')
const ENGLISH_LEVEL = require('./english.level.enum')
const REFERRAL = require('./referral.enum')
const USER_ROLES = require('./user.roles.enum')

module.exports = {
  COURSES,
  ENGLISH_LEVEL,
  REFERRAL,
  USER_ROLES
}
