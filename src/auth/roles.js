const CANDIDATE = 'candidate'
const STUDENT = 'student'
const MANAGER = 'manager'
const ADMIN = 'admin'

const ALL = [CANDIDATE, STUDENT, MANAGER, ADMIN]

module.exports = {
  CANDIDATE,
  STUDENT,
  MANAGER,
  ADMIN,
  ALL
}
